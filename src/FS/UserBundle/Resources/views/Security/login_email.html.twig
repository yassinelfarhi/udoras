<div style="text-align:center">
    <img src="{{ absolute_url(asset('bundles/fsudoras/images/logo-copy.png')) }}" alt="Logo"/>
    <h2>Welcome to uDoras!</h2>
    <br/>
    {% if 'ROLE_VENDOR' in user.getRoles() %}
        You have been registered as a vendor.
    {% elseif 'ROLE_CUSTOMER' in user.getRoles() %}
        You have been registered as a customer.
    {% else %}
        You have been registered as an employee.
    {% endif %}
    <br/>
    Your login is: {{ user.email }}
    <br/>
    Please, <a href="{{ loginLink }}">Set your password</a>
</div>